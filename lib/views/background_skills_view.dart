
import 'package:flutter/material.dart';
import 'package:travchar/model/Character.dart';

class BackgroundSkillsView extends StatelessWidget {
  final String nextRoute;
  final Character character;

  BackgroundSkillsView(this.nextRoute, this.character);

  @override
  Widget build(BuildContext context) => Text("woo");
}